{
  "name": "cobol-instrumented-interpreter-poc",
  "version": "0.1.0",
  "description": "POC: Type-safe instrumented COBOL subset interpreter for modernization",
  "main": "dist/src/main.js",
  "scripts": {
    "build": "tsc",
    "start": "tsc && node dist/src/main.js",
    "test": "tsc && node dist/tests/run-tests.js"
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.3.0"
  }
}